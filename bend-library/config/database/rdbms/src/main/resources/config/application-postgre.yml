bend-platform:
    database:
        database-type: POSTGRESQL
        host: localhost
        schema: bend_platform
        username: lemon
        password: lemon
        driver: org.postgresql.Driver
        hibernate:
            dialect: org.hibernate.dialect.PostgreSQL82Dialect
