apply plugin: "war"

bootWar {
    mainClassName = "bend.BendPlatformApplication"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
